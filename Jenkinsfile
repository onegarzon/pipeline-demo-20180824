node {
  stage ('Checkout') {
    sh 'ls -a'
  }
  stage ('Static Code Analysis') {
    sh 'ls'
  }
  stage ('Build') {
    sh 'ls -alt'
  }
  stage ('Unit Testing') {
    sh 'ls -l'
  }
  stage ('Delivery') {
    sh 'ls -t'
  }
}
